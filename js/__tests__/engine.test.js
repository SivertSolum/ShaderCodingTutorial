const engine = require('../engine');

describe('engine', () => {
  test('should export an object', () => {
    expect(typeof engine).toBe('object');
  });
  // Add more tests for shader compilation and engine logic
});