const app = require('../app');

describe('app', () => {
  test('should export an object', () => {
    expect(typeof app).toBe('object');
  });
  // Add more tests for lesson loading and navigation
});